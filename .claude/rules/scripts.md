# Script Rules

## Scope
Applies when editing `scripts/**` and operational helper scripts.

## Rules
- Default scripts to safe behavior (no destructive action by default).
- Require explicit flags for destructive or irreversible operations.
- Print clear preconditions and expected outcomes.
- Keep scripts idempotent where practical.

