version: 1
updated: "2026-05-22"

current_focus:
  - "Spec 07 COMPLETE (Notifications). Transactional emails (hold-ready/welcome/rejection) + lifecycle due-date reminders (T-2/T-0/T+1) via hourly Vercel Cron (NOT Workflow DevKit — user-confirmed) + AI-drafted batch composer with librarian review. Resend + React Email. Migrations 0010 (outgoing_emails/email_batches + member/tenant column adds) + 0011 (reminder dedup partial unique index) applied. Critic cycle REQUEST_CHANGES (2 HIGH/5 MEDIUM) -> bug-fixer -> APPROVE. 70/70 unit tests, typecheck + biome + build green."
  - "Specs 01-07 all implemented + critic-approved. Demo DB seeded (tenant 'Stack Public Library' slug stack-public: 8 books, 6 members, 2 loans, 1 hold). Migrations 0000-0011 all applied to live Neon DB."
  - "Stack target architecture: Next.js 16 + Vercel + Neon Postgres + pgvector + Auth0 Organizations + Vercel AI Gateway"
  - "Multi-tenant SaaS library platform — assignment for Valsoft AI Engineering Manager role"

recent_changes:
  - "2026-05-22: Spec 07 landed — Notifications. 6 builder slices across 3 runs + a critic/bug-fixer cycle. Deps added: resend, @react-email/components, @react-email/render. Migration 0010_notifications (outgoing_emails + email_batches, RLS FORCE; members.email_status + members.lifecycle_emails_enabled; tenants.email_monthly_cap) + 0011_notifications_dedup (partial unique index on outgoing_emails (tenant_id,loan_id,email_type) WHERE loan_id IS NOT NULL; email_batches.updated_at). lib/notifications/*: email-client (Resend + dev-outbox fallback, never throws on missing key), 6 React Email templates, interpolate + unsubscribe-token (HMAC) pure helpers, ai-draft (PII-free aggregates only — NFR-07-05), transactional/lifecycle senders, volume-cap, reminders (3 date windows + NOT EXISTS dedup), triggers facade, send-batch (chunks of 50). app/api/cron/send-reminders (hourly, mirrors expire-holds) + vercel.json entry. app/api/webhooks/resend (hand-rolled Svix HMAC verify, bounce/complaint suppression, 503-on-missing-row). Public app/unsubscribe page. app/(app)/notifications compose-batch UI + Server Actions (email:compose/email:send CASL perms). SCHEDULING DECISION: hourly cron, not Vercel Workflow DevKit (user-confirmed). Critic 2 HIGH (network sendEmail inside open DB tx -> mid-batch failure rolled back audit rows) + 5 MEDIUM -> bug-fixer refactored every send path to load->send->record short-tx shape -> APPROVE. 70/70 unit tests, build green 25 routes."
  - "2026-05-22: Spec 06 landed — Reader's Advisor (conversational RAG chat). 6 builder slices across 3 runs. Migration 0009_chat (chat_threads/chat_messages/chat_refusals/ai_usage, all RLS FORCE). lib/ai: streamTextViaGateway, routing.ts MODELS table, readers-advisor.v1.md prompt + load-prompt parser, lint-prompts.mjs, 4 tools each in own withTenantTx (place_hold CASL-gated). lib/domain/chat/* persistence + pure helpers (page-context, book-refs, refusal). app/api/chat/stream/route.ts (auth 401, flag 404, budget 402, friendly 503). UI: app/(app)/chat + components/chat/* (useChat via @ai-sdk/react 3.0.189), components/command-palette/* (⌘K, 250ms debounce, 3 options), sidebar Ask Stack entry. evals/readers-advisor.dataset.json (25 dialogues + 3 cross-tenant probes). Critic REQUEST_CHANGES 3 HIGH/1 MEDIUM -> bug-fixer -> re-critic APPROVE (H1 friendly-503 dead code -> toUIMessageStreamResponse onError; H2 unvalidated messages -> safeValidateUIMessages + 422 + server-side soft-truncate; H3 unsound refusal heuristic -> isRefusalText phrase match). 63/63 unit, build green 21 routes."
  - "2026-05-22: Spec 05 landed — Search & Discovery. Run A backend (25 files): migration 0008 (tsvector GENERATED column, pgvector book_embeddings + HNSW, trigram indexes, search_zero_result_log, all RLS FORCE), lib/domain/search/* (rrf, lexical/semantic/hybrid search, books-like-this, facets, embed-book), generateEmbedding via AI Gateway, POST /api/search. Run B UI (11 files): /search RSC page, debounced search-box, facet sidebar, zero-results, Books-like-this rail. Migration needed 2 orchestrator immutability fixes (books_search_tsv + immutable_array_to_string IMMUTABLE wrappers). Critic REQUEST_CHANGES 3 HIGH/4 MEDIUM → bug-fixer → re-critic APPROVE. 52/52 unit, build green."
  - "2026-05-22: Combined chain — Spec 04 Run B (member UI: signup, approval queue, member list/edit, profile), Spec 03 Run C (Vercel hourly cron for expireStaleHolds + with-system-tenant-tx system path), F-2 design-token sweep (32 files arbitrary [hsl(var())] → @theme utilities + IsbnBanner extraction + aria-hidden). Critic 3 HIGH/7 MEDIUM → bug-fixer (timingSafeEqual cron auth, OCC token fix, last-admin invariant on status change). 52/52 unit, build green 17 routes."
  - "2026-05-22: DB plumbing — migrations 0006+0007 (hand-written, never applied) applied via scripts/apply-migration.mjs. New scripts/seed-demo.mjs + pnpm db:seed creates demo tenant (Stack Public Library) with 8 books + 6 members. seed-circulation hardened to pick a tenant with real data. DATABASE_URL confirmed live."
  - "2026-05-22: Spec 03 Run B landed — loans/holds UI surface. 4 client components: RenewButton (OPTIMISTIC_CONCURRENCY + RENEWAL_BLOCKED_BY_HOLD + RENEWAL_LIMIT_REACHED handling), ReturnButton (confirmation dialog), CancelHoldButton (confirmation dialog), PlaceHoldButton (HOLD_ALREADY_EXISTS + HOLD_NOT_PLACEABLE as informational toasts). /loans RSC: librarians see paginated table with overdue filter + return button (listActiveLoans); members see own card list (listLoansByMember) guarded by getMemberByUserId stub. /holds RSC: librarians see per-book queue (?bookId param) or guidance; members see own holds (listHoldsByMember) guarded by stub. Book detail: isCheckedOut badge, hold queue section, PlaceHoldButton (gated on currentMember). Sidebar Loans + Holds enabled. getMemberByUserId stub: returns null until Spec 04 adds auth0_user_id. 0 new tests (Load-Bearing Filter: canRenew already covered, no RTL for component tests). 33/33 unit, 0 biome, 0 type errors."
  - "2026-05-21: Spec 03 Run A landed — circulation backend. Schema: loans + holds tables, members rewritten (status enum + updatedAt; Spec 04 adds auth0_user_id), _shared.ts gets MemberId/LoanId/HoldId brands. Migration 0006_circulation.sql hand-written (DB unavailable in agent env) with RLS FORCE, composite tenant-first indexes, partial UNIQUE for hold dedup. Domain: 16 modules across loans/ + holds/ — borrow-book uses FOR UPDATE, promote-next-hold uses SKIP LOCKED, renew-loan uses optimistic concurrency. has-active-loan stub REPLACED with real query (closes Spec 02 follow-up). Server Actions for borrow/return/renew/place-hold/cancel-hold. 10 new error→ProblemDetails-code mappings in safe-action. librarian role gets hold:delete. 3 new unit tests on pure helpers (computeDueAt + canRenew). 33/33 unit, 0 biome, 0 type errors. 6 integration tests + Vercel Workflow trigger + 5 domain events deferred to follow-ups."
  - "2026-05-21: NEW-1 (auditlog AppSubject) fixed — parsePermission special-cases 'auditlog'→'AuditLog' the same way 'ai'→'AI'. Regression test added in tests/unit/auth/ability.test.ts. Was silently dropping system_owner.can('read','AuditLog') with only a console.warn."
  - "2026-05-21: DATABASE_URL validation hardened in lib/db/client.ts — now rejects placeholder values (e.g. '<neon-pooled-stack_app>') with a clear error pointing to the correct Neon connection-string format. Was: opaque 'TypeError: Invalid URL' deep in the Pool constructor on the first DB call."
  - "2026-05-21: Spec 02 Run B-fix landed — 5 critic findings fixed via bug-fixer-sonnet. F-1 text-[11px]→text-caption (brief 12px floor) in new-book-form.tsx + [id]/page.tsx. F-3 cover shadow+border violation: dropped border, kept elev-2. F-4 Sidebar.canViewTrash prop wired from (app)/layout via buildAbility(session.roles).can('delete','Book'). F-5 dev bypass: reads DEV_BYPASS_TENANT_ID env first; one-time console.warn naming resolved tenant; throws new DevBypassNoTenantsError (in errors.ts, mapped to 503 in safe-action.ts) instead of zero-UUID fallback. F-6 ProblemDetails.code field added; handleServerError emits stable code per error class (OPTIMISTIC_CONCURRENCY, TENANT_NOT_PROVISIONED, etc.); edit-form switches on err.code not err.status===409. 0 type errors, 0 biome, 27/27 unit tests."
  - "2026-05-21: Spec 02 Run B landed — full books UI surface. Tailwind v4 CSS-first config (globals.css + @theme), DEV_AUTH_BYPASS stub session, app shell (sidebar + topbar + layout), books list RSC, book detail RSC, new-book form with ISBN preview (previewIsbnAction → selectIsbnBanner), edit form with optimistic concurrency (expectedUpdatedAt → 409 banner), trash view (semantic table, 30-day countdown), loading/error/not-found boundaries for all routes. 7 new unit tests (cover-color + isbn-banner). 27/27 unit tests, biome + typecheck green."
  - "2026-05-21: Spec 02 Run A-fix landed — critic findings F-1 through F-11 addressed. previewIsbnAction permission book:read→ai:use_enrich (HIGH). Integration test afterAll trigger-safe cleanup (HIGH). assertAiBudget moved outside try in preview-isbn (MEDIUM). isNull(deletedAt) predicate added to updateBook+softDeleteBook WHERE (MEDIUM). _shared.ts timestamps→precision:3 + migration 0005 + 1 unit test for ms round-trip (MEDIUM). Dropped non-load-bearing merge null test (MEDIUM). Renamed misleading isbn test (LOW). Removed redundant sequence GRANT in 0004 (LOW). tsc PASS, biome PASS, 20/20 unit tests, 13/13 db:verify."
  - "2026-05-21: Spec 02 Run A landed — books + isbn_cache tables with RLS, ISBN validation, OL+GB fan-out with merge/sourcesDiff, isbn_cache, preview-isbn, CRUD domain functions, Server Actions, AI gateway + budget stubs. 13/13 db:verify (incl. Spec 02 check #12), 20/20 unit tests, biome + typecheck green. `ai` v6 added. BookId brand in _shared.ts. handleServerError extended with book domain errors."
  - "2026-05-21: Auth0 JWT shape pivot — roles in JWT, ROLE_PERMISSIONS table in lib/auth/permission.ts resolves to permissions. Forced by Auth0's `event.authorization` type (only carries roles[], no permissions[]). buildAbility now takes roles[]; Session.roles replaces Session.permissions. Action snippet in tests/integration/_README.md fixed. 12/12 db:verify, 9/9 unit, biome + typecheck green."
  - "2026-05-21: Spec 01 Run B-fix landed — critic caught the builder's own deferral as a runtime CRITICAL (org_id text vs uuid). Resolver + cache + audit override + metadata gate + handleServerError branches all done. 12/12 db:verify, 9/9 unit, biome + typecheck green."
  - "2026-05-21: Spec 01 Run B landed — Auth0 wiring + CASL Ability + audit writer + tenant provisioning + 0003 tenants RLS migration. 8/8 unit tests. Critic-opus flagged 1 CRITICAL + 3 HIGH + 6 MEDIUM."
  - "2026-05-21: Spec 01 Run A hardening — verify-foundation.mjs caught 3 foundation bugs the mocks missed: SET LOCAL doesn't accept $1 placeholders (→ set_config), custom GUC params don't raise on unset (→ assert_tenant function), Neon's default role has BYPASSRLS=true (→ stack_app role). All 8 integration checks now pass against real Neon branch."
  - "2026-05-21: Spec 01 Run A landed — Next.js 16 scaffold + Drizzle schema + RLS migration + withTenantTx (neon-serverless WS Pool) + audit_log append-only trigger + 3 unit tests. Critic-caught CRITICAL fixed in same run."
  - "2026-05-21: .claude/CLAUDE.md rewritten for Next.js 16 stack; backend.md + migrations.md rules rewritten in the same pass"
  - "2026-05-21: Working titles locked (Stack name, community/public library demo, ⌘K + sidebar advisor surface)"
  - "2026-05-21: 12 feature specs + INDEX written under project_docs/specs/"
  - "2026-05-21: Memory updated with Valsoft context, locked spec decisions, and feature-map"
  - "2026-05-21: 5 analysis docs + design brief authored under docs/analysis/ + docs/design/"

open_blockers:
  - "Phase-0: Auth0 Organizations tier availability must be verified before any auth code is written. Owner: user. Fallback: Clerk (free tier with Organizations) or tenant_id-column model from docs/analysis/04-multi-tenant-data-model.md."

decisions_recent:
  - id: "D-2026-05-21-stack-pivot"
    decision: >
      Pivot from .NET 10 / EF / Vercel to Next.js 16 full-stack on Vercel + Neon Postgres + pgvector.
      Rationale: Vercel does not host .NET; we need a single deployable on Vercel for the demo URL.
    refs:
      - "docs/analysis/01-radar-analysis.md"
      - "docs/analysis/03-tech-stack-decisions.md"
  - id: "D-2026-05-21-solution-A-single-app"
    decision: >
      Adopt Solution A: single Next.js app, all state in Postgres (incl. pgvector embeddings), Auth0 Organizations
      as tenant boundary, four-layer isolation (auth → repo guard → SET LOCAL → RLS FORCE).
    refs:
      - "docs/analysis/01-radar-analysis.md §Recommendation"
      - "project_docs/specs/01-foundation-multi-tenancy-auth.spec.md"
  - id: "D-2026-05-21-ai-tier1"
    decision: >
      Lock AI Tier-1 feature set: hybrid catalog search, Reader's Advisor (RAG + tool calls), ISBN-to-record
      enrichment. Tier-2 ships if time allows; Tier-3 personalized recs is post-MVP.
    refs:
      - "docs/analysis/02-ai-features-research.md"
      - "project_docs/specs/05-search-discovery.spec.md"
      - "project_docs/specs/06-readers-advisor.spec.md"
      - "project_docs/specs/02-book-management.spec.md"
  - id: "D-2026-05-21-roles-and-signup"
    decision: >
      Role hierarchy: System Owner / Tenant Admin / Librarian / Member / Guest. Demo tenant pre-seeded with one
      account per role. Member self-signup is a public form per tenant; status='pending' → librarian approval
      queue → welcome email on approve / rejection email on reject.
    refs:
      - "project_docs/specs/04-member-management.spec.md"

bugs_open: []

patterns_active:
  - "Spec-driven workflow: docs/analysis (RADAR) → project_docs/specs (EARS+BDD) → builder TDD → critic review → CI gates"
  - "Four-layer tenant isolation: Auth boundary → repo guard → SET LOCAL app.tenant_id → RLS FORCE"
  - "CQRS in Next.js: Server Actions = commands (next-safe-action + Zod); RSC fetches/Route Handlers = queries"
  - "AI calls routed through Vercel AI Gateway; Langfuse spans on every call; per-tenant cost cap pre-check"
  - "Evals as CI gates; cross-tenant probe in every AI eval set"

next_steps:
  - "User-side UI smoke test: set DEV_AUTH_BYPASS=1 + DEV_BYPASS_TENANT_ID=f3d96d5e-dd55-4e72-a545-e7322eb8a359 (the seeded Stack Public Library tenant) in .env.local; pnpm dev; exercise /books, /loans, /holds, /members (+ pending/edit/me), /signup, /search, /chat + the Cmd-K palette. Report visual/UX bugs."
  - "Spec 06 needs AI_GATEWAY_API_KEY in .env.local for the Reader's Advisor to actually stream — without it /api/chat/stream returns the friendly 503. Set it to demo the chat live."
  - "Spec 07 env (.env.local — file is permission-blocked for the agent, user must add): RESEND_API_KEY (real send; absent = dev-outbox console fallback, app stays demoable), RESEND_FROM_EMAIL (verified sender, fallback onboarding@resend.dev), RESEND_WEBHOOK_SECRET (whsec_… — absent = dev mode skips webhook signature verify), UNSUBSCRIBE_SECRET (HMAC key for unsubscribe links), APP_URL (base URL for unsubscribe links). CRON_SECRET (already noted) now also gates /api/cron/send-reminders."
  - "Spec 07 follow-ups: integration test for the H1 scenario (volume cap trips mid-batch -> already-recorded outgoing_emails rows survive) — the one failure mode unit tests cannot reach; React Email template client-rendering check (NFR-07-04 — Gmail/Outlook/Apple Mail) deferred, no CI snapshot suite; calendar-day vs wall-clock semantics for audience windows (critic L2 — 'overdue 5+ days' currently uses 5×24h from now, confirm with product); per-locale FR/EN templates (Q-07-02), SMS (Q-07-01), per-librarian daily AI-draft cap (Q-07-04) all out of scope v1; Langfuse tracing for the patron_email_draft span is Spec 11."
  - "Spec 06 follow-ups: thread-archival cron for chat_threads >30d idle (REQ-06-09 — archived_at column shipped, worker deferred; mirror Spec 03 Run C cron pattern); partial-response persistence on mid-stream disconnect (spec §9 — onFinish does not fire on dropped connection); the eval CI runner + Langfuse tracing are Spec 11 (evals/readers-advisor.dataset.json is the dataset artifact only); per-tenant kill switch via Edge Config (REQ-06-10 — currently global env flag, per-tenant deferred to Spec 11); ai_usage MTD accumulation enforcement in assertAiBudget (Spec 11 REQ-11-03)."
  - "Spec 05 follow-ups: ts_headline snippet highlighting in search results; REQ-05-08 nearest-3 zero-result suggestions (hybridSearch must return them); integration tests IT-05-*; eval set pnpm eval:search (nDCG@5 >= 0.65 CI gate); public-catalog lexical-only search mode (Spec 09); Workflow-based async re-embed backfill."
  - "FOLLOW-UP (LOW): hybrid-search.ts imports db directly for the fire-and-forget zero-result log (deliberate, isolation preserved via set_config); search-zero-result-log.ts:6 doc comment is stale after the H3 refactor."
  - "FOLLOW-UP (F-2 residual, LOW): 9 alpha-modified [hsl(var())] tokens left // REVIEW: in the design sweep — need oklch/rgb @theme tokens before they can use Tailwind opacity modifiers."
  - "FOLLOW-UP (F-7, LOW): Surface AI provenance in ISBN preview success banner ('Enriched via Open Library + Google Books')."
  - "FOLLOW-UP (F-10, LOW): Clarify getBook deletedAt semantics — detail page checks book.deletedAt but if getBook filters deleted rows by default this is dead code."
  - "FOLLOW-UP (F-9 isbn, LOW): restore-book.ts error specificity — distinguish not-found vs not-deleted vs past-retention-window."
  - "FOLLOW-UP (F-12, LOW): preview-isbn.ts — validate cache write payload through BookRecordSchema.partial().safeParse() before putCached."
  - "FOLLOW-UP (Spec 04 M7, deferred): FU-04-B-3 — requires DB migration + UX design (see spec-multi-fix diff summary)."
  - "Deferred integration tests: IT-03-1..7 (circulation), IT-04-* (member management), IT-05-* (search). Run against a real Neon branch before merge."
  - "Migration journal: drizzle/meta/_journal.json does not exist — migrations are applied via scripts/apply-migration.mjs per-file, not drizzle-kit migrate. Migrations 0000-0011 all applied to live DB as of 2026-05-22."
  - "Test pollution: ~26 leftover 'Verify A/B' integration-test tenants in the live DB (integration test cleanup does not delete tenant rows). Harmless but noisy; consider a cleanup script."
  - "User: deploy Auth0 Post-Login Action in dashboard so JWT carries org_id + roles[]. Corrected action code is in tests/integration/_README.md. Required for end-to-end login flow."
  - "User: in Auth0 dashboard → Organizations → <demo org> → Roles, create the five roles system_owner / tenant_admin / librarian / member / guest; assign at least one org member tenant_admin."
  - "User: rotate stack_app password before any non-local deploy."
  - "User: set CRON_SECRET env var in Vercel for the /api/cron/expire-holds hourly cron (vercel.json)."
  - "Set up GitHub repo with CODEOWNERS, branch protection JSON, secrets, labels per Spec 12 §10."
